/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseDtoMedicalProcedure } from '../../models/page-response-dto-medical-procedure';

export interface GetPaginatedProcedures$Params {

/**
 * Índice de la página a recuperar.
 */
  pageIndex?: number;

/**
 * Tamaño de la página.
 */
  pageSize?: number;
}

export function getPaginatedProcedures(http: HttpClient, rootUrl: string, params?: GetPaginatedProcedures$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseDtoMedicalProcedure>> {
  const rb = new RequestBuilder(rootUrl, getPaginatedProcedures.PATH, 'get');
  if (params) {
    rb.query('pageIndex', params.pageIndex, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseDtoMedicalProcedure>;
    })
  );
}

getPaginatedProcedures.PATH = '/mdm/procedure';
