/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseDtoSuperAdmin } from '../../models/page-response-dto-super-admin';

export interface GetPaginatedSuperAdmins$Params {

/**
 * Índice de la página a recuperar.
 */
  pageIndex?: number;

/**
 * Tamaño de la página.
 */
  pageSize?: number;
}

export function getPaginatedSuperAdmins(http: HttpClient, rootUrl: string, params?: GetPaginatedSuperAdmins$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseDtoSuperAdmin>> {
  const rb = new RequestBuilder(rootUrl, getPaginatedSuperAdmins.PATH, 'get');
  if (params) {
    rb.query('pageIndex', params.pageIndex, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseDtoSuperAdmin>;
    })
  );
}

getPaginatedSuperAdmins.PATH = '/super-admin/';
