/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InvitationResponse } from '../../models/invitation-response';

export interface ValidateToken1$Params {

/**
 * One-time token from the invitation URL.
 */
  token: string;
}

export function validateToken1(http: HttpClient, rootUrl: string, params: ValidateToken1$Params, context?: HttpContext): Observable<StrictHttpResponse<InvitationResponse>> {
  const rb = new RequestBuilder(rootUrl, validateToken1.PATH, 'get');
  if (params) {
    rb.query('token', params.token, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InvitationResponse>;
    })
  );
}

validateToken1.PATH = '/auth/invitations/validate';
